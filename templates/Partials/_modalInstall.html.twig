<div id="pwa-install" class="text-right">
    <a href="#" onclick="installApp();return false;"><span class="badge badge-info">Nouveau !</span> Installer l'application</a>
</div>


<div class="modal fade" tabindex="-1" id="modal-install">
    <div class="modal-dialog modal-dialog-centered modal-fullscreen">
        <div class="modal-content">
            <div class="modal-header bg-light">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h5 class="modal-title">Installer l'application</h5>                
            </div>
            <div class="modal-body">   
                <p>Pour accéder plus facilement à l’application, vous pouvez l’installer sur votre écran d’accueil.</p>
                <div class="blockquote displayIOs">
                    <div class="badge bg-info">IPhone</div>
                    <ol>
                        <li class="my-2">Appuyez sur le bouton Partager (le carré avec la flèche ⬆️)</li>
                        <li class="my-2">Faites défiler puis choisissez “Ajouter à l’écran d’accueil”</li>
                        <li class="my-2">Validez</li>
                    </ol>
                </div>
                <div class="blockquote displayChromiumBased">
                    <div class="badge bg-info">Chrome</div>
                    <ol>
                        <li class="my-2">Appuyez sur le bouton <button class="btn btn-light py-0">⋮</button> en haut à droite du navigateur</li>
                        <li class="my-2">Choisissez “Enregistrer et partager”</li>
                        <li class="my-2">Choisissez “Installer Easyamap” (ou "Installer la page en tant qu'appli")</li>
                        <li class="my-2">Validez</li>
                    </ol>
                </div>

                <p>Une fois installée, l’application apparaîtra comme une app classique sur votre téléphone / PC / tablette.</p>
                <div class="border border-danger p-2 displayNotChromiumBased">
                    <p><i class="glyphicon glyphicon-exclamation-sign text-danger"></i></p>
                    <p>Pour effectuer l'installation, il est nécessaire d'utiliser un navigateur <b>Chrome</b> (ou un autre navigateur basé sur Chromium). Merci d'ouvrir cette page dans une autre navigateur pour continuer.</p>                        
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
</div>


<script>
let isAppInstalled = true;
let deferredPrompt = null;
window.addEventListener('beforeinstallprompt', (e) => {
    e.preventDefault(); 
    deferredPrompt = e;
});



class Utils {
  static isIOs() {
        return /iphone|ipad|ipod/.test(navigator.userAgent.toLowerCase());
  }

  static isChromiumBased() {
    return /chrome|crios|crmo|edg|opr|brave\//.test(navigator.userAgent.toLowerCase())
    }

    static isPwaInstalled() {
        return window.matchMedia('(display-mode: standalone)').matches || window.navigator.standalone === true || window.Capacitor?.isNativePlatform()
    }
}




document.addEventListener("DOMContentLoaded", (event) => {
        $("#pwa-install").hide();
        window.addEventListener('appinstalled', () => {
            $("#pwa-install").hide();
        })
        if (Utils.isPwaInstalled()) {
            $("#pwa-install").hide();
        }
        else {
            $("#pwa-install").show();
        }


        $(".displayIOs, .displayNotIOs, .displayChromiumBased, .displayNotChromiumBased").hide();
        if (Utils.isIOs()) {
            $(".displayIOs").show();
        }
        if (Utils.isChromiumBased()) {
            $(".displayChromiumBased").show();
        }
        else {
            $(".displayNotChromiumBased").show();
        }

});
  
const installApp = ()=>{
    showSpinner();
    if (deferredPrompt) {  
        try {

            const timeout = setTimeout(() => {
                if (deferredPrompt) {
                    hideSpinner();
                    $("#modal-install").modal("show");
                }
            }, 5000);

            deferredPrompt.prompt(); 
            deferredPrompt.userChoice.then((choice) => {
                deferredPrompt = null;
                clearTimeout(timeout);
                hideSpinner();
            });
        } catch(e) {
            hideSpinner();
            $("#modal-install").modal("show");
        }
        return ;
    }
    hideSpinner();
    $("#modal-install").modal("show");

};

</script>

