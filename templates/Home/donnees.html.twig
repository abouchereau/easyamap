{% extends 'layout.html.twig' %}
{% block header_title %}Données{% endblock %}


{% block body %}
    <h1>Sauvegardes SQL</h1>
    <div class="row">
        <div class="col">
            <ul class="list-group">
                {% for backup in backups %}
                    <li class="list-group-item"><a href="{{ path('download_backup', {'file':backup}) }}">{{ backup }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}