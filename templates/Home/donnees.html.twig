{% extends 'layout.html.twig' %}
{% block header_title %}Données{% endblock %}


{% block body %}
    <h3>Sauvegardes SQL</h3>
    <div class="row">
        <div class="col">
            <div class="list-group">
                {% for backup in backups %}
                    <a class="list-group-item" href="{{ path('download_backup', {'file':backup}) }}">{{ backup }}</a>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}